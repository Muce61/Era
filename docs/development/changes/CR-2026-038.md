# CR-2026-038 — Read-only historical funding acceptance

## Metadata

- status: APPROVED / LOCAL HISTORY HUMAN ACCEPTED / MONTHLY RECONCILIATION WAIVED
- requested_by: Muce
- approved_by: Muce
- approved_at: 2026-07-23
- affected_task: S2P13-T11
- affected_open_question: OQ-S2-010
- formal_lifecycle_run_authorized: false
- stage3_locked: true

## Decision

The existing read-only BTCUSDT and ETHUSDT funding files under
`/Users/muce/1m_data/derivatives_data` may be received as candidate historical inputs. Every
calendar month must be independently compared to the corresponding Binance official USD-M monthly
`fundingRate` archive and its published checksum.

If a candidate row differs from the official archive, the new accepted evidence uses the official
row. The legacy candidate file is never overwritten, moved or deleted. Every discrepancy remains
counted and reported.

Acceptance evidence is written only to a new append-only directory under
`/Volumes/FuckingLife/era100x_stage2/funding-evidence`. It must contain:

- an accepted BTCUSDT and ETHUSDT dataset;
- source and official archive hashes;
- a self-hashed Funding Manifest and Catalog;
- strict consumer read-back and an independently reproducible Verify record;
- comparison counts, date boundaries and explicit legacy-source non-mutation;
- `historical_evidence_only=true`, `lifecycle_run_created=false` and `stage3_locked=true`.

## Execution order

1. Tests and the evidence consumer are completed first.
2. A real seven-complete-UTC-day rehearsal runs over `[2020-01-01, 2020-01-08)`.
3. The rehearsal must perform download, checksum validation, comparison, serialization, strict
   read-back, reconciliation, Verify and read-only UI projection.
4. The rehearsal plus a complete local structure/Hash audit may be human-accepted without a
   month-by-month official full-history reconciliation.
5. Muce explicitly waived the monthly reconciliation and separate preflight-binding requirement
   on 2026-07-23. This does not waive source Hash, continuity, BTC/ETH separation or truth labels.

## Prohibitions

- No lifecycle Authority, Binning Snapshot, Run ID, resume or publication.
- No modification of Stage 1, T10-T15 evidence or existing funding files.
- No account API, user data, testnet, order or real-funds connection.
- No inferred zero funding and no replacement of missing official months with Stress assumptions.
- No S2P13-T12+ execution and no Stage 3 unlock.

## Seven-day result

The real `[2020-01-01, 2020-01-08)` rehearsal accepted 21 official rows per instrument and passed
strict read-back and independent Verify. It identified three local settlement timestamps per
instrument rounded down by 1-2 milliseconds. The new accepted evidence preserves official
timestamps; both legacy files remain byte-identical. The evidence is append-only at
`funding-evidence/s2p13-t11-funding-7d-cr-2026-038-v1`.

Muce subsequently accepted the existing complete local history: 7,128 unique, consecutive
eight-hour rows per instrument from 2020-01-01 through 2026-07-03 16:00 UTC. Acceptance Hash:
`91ebd6e5433829a0085bc2cb9871d33d383f56a9626ac7c57b79abc7e6127d89`.

`HISTORICAL_FUNDING` is satisfied without claiming every month was reconciled row-by-row to the
official archive. This does not authorize lifecycle execution.
