# CR-2026-026 — S2-T15 v1.4 Executable Conditional-Baseline Contract

## Metadata

- change_level: L3_TASK_CONTRACT_AND_PREREGISTRATION_EXTENSION
- status: APPROVED / IMPLEMENTATION READY / CR-2026-027 VALIDATED
- approved_by: Muce
- approved_at: 2026-07-22T02:25:41Z
- affected_stage: Stage 2
- affected_task: S2-T15
- task_version: 1.4
- decision_record: ADR-S2-009
- resolves: OQ-S2-005
- supersedes_contract_gap_only: CR-2026-025 post-approval blocker
- formal_spec_change: NONE
- source_data_change: NONE
- later_task_authorization: NONE

## Decision

Muce approved the complete executable S2-T15 v1.4 contract in the task conversation. This record
preserves CR-2026-025 and its original approval/failure history; it does not rewrite or delete it.
The approved contract is an implementation of manual §13.1/§13.3, not a modification of
`docs/spec/**`.

The following values are frozen before any S2-T15 Authority or Run ID:

- `VOLATILITY_1M_60BAR_RMS_BPS_V1`: 61 complete causal 1m closes, 60 simple-return bps,
  uncentered/unannualized RMS, Decimal precision 50, HALF_EVEN, quantum `1e-18 bps`;
- `TRADES_ACTIVITY_COUNT_60S_V1`: complete `[cutoff-60s, cutoff)` Trade-second count; proven
  complete zero-Trade windows equal zero, missing coverage is unavailable;
- setup `KEY_LOW_SWEEP_RECLAIM_HOLD_V1@1.0` and context `CAUSAL_EMA20_1H@1.0`; controls delegate
  to the accepted S2-T07 closed-1H causal EMA20 implementation;
- `DISTANCE_TO_ACTIVE_CANONICAL_KEY_LEVEL_BPS_V1` is enabled and its quintile is exact through
  L0-L4; controls use same-instrument/same-parameter ACTIVE levels with causal availability and
  tie order `(distance_bps, priority, key_level_id)`;
- P1/P2/P3 use five fixed UTC blocks and expanding F0-F3 folds; F3 is the only HOLDOUT and B0
  events are `TRAIN_ONLY_NOT_EVALUATED`;
- purge is 3600 seconds, outcome embargo is 600 seconds and the full information span is
  `[anchor-3600s, anchor+600s)`;
- the one-minute daily-offset grid and matching seed are `20260716`;
- anchor, candidate and outcome matrix identities are separate; candidate identity excludes
  label/outcome/Run ID;
- each H2 path is matched once, exactly five selected controls are shared across all 30 registered
  target/stop cells, and outcome reading occurs only after outcome-blind selection;
- T13 is the event row-label source. T14 is aggregate-policy-only and is bound through its
  Manifest/Catalog/Verify evidence; no T14 Episode join exists;
- complete H2 windows with zero Trades are `AMBIGUOUS / NO_OBSERVATIONS / strict=0`, not EXPIRED;
- all global and grouped Episode/control/cell equations are fail-closed; a mismatch is
  `FAILED_UNPUBLISHED` and cannot produce UI PASS.

## Authority gate

Governance approval closes the definition gap but does not pre-accept implementation or unknown
research results. Authority creation additionally requires final code, directed tests and the
repository quality gate to pass. Binning snapshots require TRAIN-only inputs. A Run ID may first
be created only by `run`, after `audit`, `freeze-authority`, `freeze-bins` and `preflight` pass.

## Append-only implementation audit finding

The mandatory v1.4 audit at `2026-07-22T03:00:27Z` returned `BLOCKED` before Authority or Run.
The fixed T10 Catalog has 14,256 required Group-1 receipts without the `field.*` distribution
digests declared by their DatasetSpecs: 4,752 `canonical_key_levels` and 9,504 PRICE/FLOW
`market_episodes` partitions. The current authority `CatalogReaderV2` consequently fails closed on
those inputs. Upstream binding Hash:
`a1f73a8f115262efa47f735593d4b142493e2baede551f0a193ff82ea7929f92`.

This finding does not revoke the approved v1.4 method contract and does not alter T10. It opens
OQ-S2-006 and blocks Authority, bins, Run and UI PASS until an append-only input/receiver decision
is approved and a new audit passes. Bypassing the reader or silently treating the missing digests
as valid is not authorized.

Muce approved the independent [CR-2026-027](CR-2026-027.md) receiver supplement at
`2026-07-22T03:24:18Z`. The first supplement verified all 14,256 affected partitions without
modifying T10 and the following audit passed. A final clean-code commit and a fresh audit bound to
the final governance Hashes remain mandatory before Authority or bins.

## Allowed paths

- `src/era100x/research/stage_2/baselines/conditional/**`
- `tests/research/stage_2/baselines/conditional/**`
- `scripts/run_stage2_conditional_baseline.py`
- CR-approved read-only Stage 2 progress projection and its tests
- S2-T15 Task/Validation, T19 append-only addendum, OPEN_QUESTIONS, CURRENT_STAGE,
  TRACEABILITY and the S2-T15 direct rule state
- new append-only `/Volumes/FuckingLife/era100x_stage2/{authorities,runs}/S2-T15` evidence

## Forbidden scope

No Stage 1 or T10/T11/T13/T14 sealed mutation, `docs/spec/**`, S2-T16+, Stage 3, trading code,
network/account/funds, bootstrap, CI, F1, PnL, ROUND_SUCCESS or live-return claim is authorized.
