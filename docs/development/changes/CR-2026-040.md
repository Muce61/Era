# CR-2026-040 — Release OQ-S2-009 and classify the seven-day rehearsal as an execution gate

## Metadata

- status: `APPROVED / OQ-S2-009 RESOLVED / SEVEN-DAY REHEARSAL AUTHORIZED`
- approved_by: Muce
- approved_at: `2026-07-23`
- affected_open_question: `OQ-S2-009`
- new_execution_gate: `FINAL_CODE_7_DAY_REHEARSAL`
- formal_lifecycle_run_authorized: `false`
- Stage 3: `LOCKED`

## Decision

`OQ-S2-009` is resolved because its missingness treatment and release behavior are no longer
unknown:

- typed boundary warmup and declared gaps are recorded explicitly;
- complete zero activity and zero observations remain valid observed states;
- unknown missingness, unbound partitions and source/receipt/Hash drift fail closed;
- no imputation, shortened window, silent skip or missing-as-zero conversion is allowed;
- final code must pass one seven-complete-UTC-day producer-to-UI rehearsal before a formal
  successor can be considered.

The last item is an approved execution specification, not an open question. It is therefore moved
from `blocking_questions` to the independent `FINAL_CODE_7_DAY_REHEARSAL` execution gate.

## Current permissions

`RUN_SEVEN_DAY_REHEARSAL` is authorized only for the isolated unpublished short-run root. It must
exercise computation, serialization, checkpoint/receipt creation, strict next-consumer read-back,
reconciliation, Verify and read-only UI projection.

The following remain forbidden until the rehearsal passes and a later commit-bound human run
receipt authorizes them:

- formal Authority creation;
- Binning Snapshot creation;
- formal preflight and Run ID creation;
- resume and publication.

The formal full-data Run must still validate and reconcile the complete range. This CR creates no
Authority, bins, Run ID or formal result and does not unlock Stage 3.

## Validation

- directed governance, rerunner and UI projection tests: `58 passed`;
- full quality gate: Ruff format/lint PASS, strict mypy PASS over 224 source files, strict
  Traceability PASS, strict governance PASS and `679 passed`;
- Web UI/API projection: open questions `NONE`,
  `FINAL_CODE_7_DAY_REHEARSAL=PENDING`, Stage 3 `LOCKED`;
- formal Authority, bins and Run created: `false`.
