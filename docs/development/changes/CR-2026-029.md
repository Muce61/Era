# CR-2026-029 — S2-T15 strict Decimal receiver and final successor recovery

## Metadata

- status: APPROVED / IMPLEMENTATION AUTHORIZED
- approved_by: Muce
- approved_at: `2026-07-22T14:12:52Z`
- task: S2-T15 v1.4
- discovered_by_run: `stage2-s2t15-conditional-20260722T120658Z-023f47cffef2`
- failed_run_status: `FAILED_UNPUBLISHED`
- failure_code: `S2_T15_CONTROL_ENTRY_PRICE_STRICT_JSON_DECIMAL`
- resolves: OQ-S2-008
- later_tasks_authorized: NONE
- live_or_testnet_authorized: false

## Evidence-backed problem

The CR-2026-028 successor passed the sealed Episode Context boundary and completed all `456 / 456`
outcome-blind matching groups. Before reading any selected control H2 outcome, post-selection
ingestion parsed each canonical candidate JSON object and passed its string-form
`control_entry_price` directly to strict `V14ControlCandidate` validation. The model correctly
requires a `Decimal` instance, so the receiver stopped on the first candidate.

This is a serialization-boundary compatibility defect, not a research-contract failure. The
canonical JSON representation, candidate identity, selected controls, matching levels, bins and
prices are unchanged. The failed Run has no control outcome matrix, descriptive summary,
reconciliation, Catalog, Manifest, Verify, publication or research delta.

## Proposed minimum correction

1. At the post-selection receiver boundary only, convert the canonical decimal string to
   `Decimal` before strict model validation; reject non-string, non-finite, non-canonical or
   non-positive values.
2. Add regression coverage that round-trips a real serialized selected candidate and proves
   tampered or non-canonical decimal values fail closed.
3. Preserve both failed Runs and all partial work; do not resume either failed checkpoint and do
   not reuse their partial output as published evidence.
4. Because code commit is an Authority invalidation condition and CR-2026-028's single successor
   authorization has been consumed, a new Authority/bin/Run chain remains forbidden without
   explicit approval of this CR.
5. If approved, run the full quality gate, invalidate the CR-2026-028 successor chain, refreeze
   Authority and TRAIN bins from the corrected clean commit, and create exactly one final
   successor-of-successor Run. No additional chain is authorized.

## Explicit non-goals

- no matching, quintile, split, Context, key-level, H2 label or reconciliation contract change;
- no mutation of Stage 1 or sealed T10/T11/T13/T14 evidence;
- no publication or PASS projection from either failed Run;
- no S2-T16+, Stage 3, PnL, live API or real-funds work;
- no in-place code override under the old Authority.

## Approval gate

Muce explicitly approved the strict Decimal receiver correction, invalidation of the CR-2026-028
successor Authority/bin/Run chain, and creation of exactly one final replacement chain. New
Authority or Run creation remains blocked until the implementation quality gate and fresh audit
PASS.
