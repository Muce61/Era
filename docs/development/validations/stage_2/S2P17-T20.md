# S2P17-T20 Validation

## Scope

Plan v1.7 authorizes only the final read-only acceptance package. The implementation binds the
formal T11/T16/T17/T18/T19 evidence, does not recompute research evidence, does not execute T21,
and keeps Stage 3 locked.

## Current status

Implementation and the repository quality gate are complete. The full quality gate passed with
829 tests, Ruff format/lint, strict mypy, strict Traceability and strict governance checks. The
real diagnostic format smoke passed in 22.533187 seconds with six result-blind event identities,
28 gate rows, 3,420 parameter rows and 14 frequency rows, while staying below the 512 MiB RSS
limit. Because that diagnostic smoke preceded the final clean commit, it is not an approval
artifact.

The live Web UI was inspected in the in-app browser. It displays the nine T20 phases, current
`BLOCKED` engineering state, source-derived `PRIMARY_FAILED`,
`INCONCLUSIVE_SOURCE_GAP_CENSORING`, `NO_GO_CURRENT_EVIDENCE`, five-second refresh notice and
`Stage 3 LOCKED` without horizontal overflow.

A final commit-bound format smoke is still required after the clean implementation commit.
Formal Authority and Run remain forbidden until that smoke receives a matching human approval.

The expected research closure is `STAGE2_NO_GO_CURRENT_EVIDENCE`; this expectation must be
derived from the formal T19 package and must not be hard-coded into the Web UI.
