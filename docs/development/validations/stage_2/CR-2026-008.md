# CR-2026-008 Validation — Cross-Implementation Determinism and Feature-Snapshot Tiers

## Conclusion

**NOT_RUN / APPROVED_IN_PROGRESS**

[CR-2026-008](../../changes/CR-2026-008.md) is approved as the S2-T10 v1.8 deterministic
acceptance protocol and as a boundary for future separately approved event Tasks. No deterministic
runtime evidence has yet been produced in this planning worktree.

Current Group-1 PASS requires formal Run A and fresh V2 Run B to match for every dataset,
instrument, variant and UTC owner date in row count, empty state, canonical logical hash, ID and
payload association, ownership/inclusion status, research role, T1-T4, parameter set and reason
distribution. Physical path, file count, compression metadata and byte hash are independently
validated integrity facts but are excluded from cross-layout semantic equality. Any semantic
mismatch is FAIL; sampling and tolerance are forbidden.

Future Tier F/E/D behavior is not executable authority for a new event. Its tests must prove that
an event with all primitives in a frozen Feature Snapshot performs two complete event computations
without rescanning raw Stage 1 Trades, while a missing primitive fails closed pending a separately
approved Foundation extension. No S2-T11 through S2-T20 work is authorized.
