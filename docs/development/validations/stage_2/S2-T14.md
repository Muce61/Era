# S2-T14 Validation — Historical AMBIGUOUS Bounds

- Current conclusion: **S2-T14 v1.3 APPROVED / IN PROGRESS**
- Manual authority: V1.3.4 §9, §11-§16, Appendix D/J/L/N
- Human approval: Muce, 2026-07-21T13:07:08Z (`开始t14`)
- CR-2026-024 approval: Muce, 2026-07-21T13:37:13Z (`webui接入t14，然后收尾t14`)
- Dependency: S2-T13 v1.3 PASSED / HUMAN ACCEPTED
- Evidence level: `FIXTURE_HISTORICAL_AMBIGUITY_BOUNDS`
- Formal full output: **AUTHORIZED / NOT YET RUN**
- Web UI: **AUTHORIZED READ-ONLY / IN IMPLEMENTATION**
- Stage 3: `LOCKED`

## Rules and scope

Direct mappings are `EVENT-CONSUME-MARKET-EPISODE`,
`STRATEGY-V1-PRICE-ONLY-HISTORICAL`, `DATA-HISTORICAL-NO-FAKE-EXECUTION`, `INV-005`,
`INV-011`, `INV-013`, `CONTRACT-MARKET-EPISODE` and `GATE-STAGE-2`. The implementation is confined
to `src/era100x/research/stage_2/labels/ambiguity/` and its matching tests. The future H3 conditional
round bridge is explicitly not promoted by this Task.

## Delivered fixture contract

- Every output binds and validates the immutable S2-T13 first-passage hash and preserves its raw
  label and lineage.
- Primary always treats `AMBIGUOUS` as failure; conditional reporting excludes it; the theoretical
  upper treats it as success. These three views coexist and cannot substitute for one another.
- H1 same-event target+stop exposes `STOP_FIRST` pessimistic and `TARGET_FIRST` optimistic path
  labels while retaining raw `AMBIGUOUS`.
- Gaps, no observations and truncation receive only 0/1 success-indicator bounds; no path order is
  invented. Determinate H1/H2 labels collapse to one identical lower/upper result.
- Distribution summaries are isolated by instrument, evidence level and frozen parameter slice,
  reject duplicates and mixed groups, and are deterministic under input shuffle.
- PnL, return, ROUND_SUCCESS, live execution, raw-label reclassification and ambiguity deletion
  remain prohibited interpretations.

## Tests actually run

- `uv run python -m pytest tests/research/stage_2/labels/ambiguity -q`: **15 passed**.
- `uv run mypy src/era100x/research/stage_2/labels/ambiguity`: **PASS**, 3 source files.
- `uv run python scripts/run_quality_gate.py`: **PASS**.
  - Ruff format/lint: 269 files, PASS;
  - strict mypy: 183 source files, no issues;
  - strict Traceability: 32 rules, 41 INV, 18 contracts, 52 reasons and 10 gates;
  - complete pytest suite: **523 passed**.

## Approved v1.3 scope

CR-2026-024 authorizes the minimum full-data CLI, Authority/Run/Manifest/Catalog publication,
repository summary and read-only progress-Web projection. The formal evidence and post-change test
results will be appended here after execution; no result is pre-accepted.

## Gate conclusion

The v1.2 fixture implementation and repository quality gate remain **PASS**. S2-T14 v1.3 is
**IN PROGRESS** until the full-distribution, Verify, automatic UI and current repository quality
evidence pass. S2-T15 is not approved or started. Stage 3 remains locked.
