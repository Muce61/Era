# S2P13-T11 validation

## Status

`IMPLEMENTATION_IN_PROGRESS / OQ_S2_012_RESOLVED / NO FINAL REHEARSAL`

This is not Task PASS, not a formal lifecycle result and not a Stage 3 gate.

## CR-2026-041 producer-wiring checkpoint

The v2 producer handoff now carries complete Manifest, Catalog, Snapshot, scope and receipt
bindings; static and input preflight are separate; and the corrected DAG makes S2P13-T13 and
S2P13-T14 consume S2P13-T12 independently. Directed adapter/governance tests and the complete
quality gate pass with 685 tests.

CR-2026-042/ADR-S2-019 resolve OQ-S2-012. Protection/structure are explicitly not modeled;
Contract Price owns scenario valuation and funding notional; canonical Trades own target/stop; and
the two policies use independent single-position timelines. The unified producer CLI, explicit
source bindings and scoped T12–T15 production cores are implemented. No final clean-commit
seven-day rehearsal, Authority, bins, Run ID or publication has yet been created.

## Scope and rules

- Manual: `docs/spec/system_manual_v1.3.5_final.md` §14.2 and Appendix L.
- Plan: Stage 2 Plan v1.3.
- Rules: `RESEARCH-S2-CONDITIONAL-LIFECYCLE`,
  `EVENT-CONSUME-MARKET-EPISODE`, `STRATEGY-V1-PRICE-ONLY-HISTORICAL`,
  `DATA-HISTORICAL-NO-FAKE-EXECUTION`, `RESEARCH-H3-CONDITIONAL-ROUND-PROB`,
  `PNL-NO-DOUBLE-SLIPPAGE` and the frozen exit/risk/round boundaries.

## Implemented evidence

- deterministic paired immediate/continue lifecycle core;
- Decimal activation, near-zero, scenario-cost and quantity-close contracts;
- target/stop/scenario-liquidation/censor outcomes, with protection/structure explicitly
  `NOT_MODELLED_STAGE2`;
- typed availability audit with reconciliation and independent Verify;
- Plan-namespaced task identity, fail-closed SRP consumer boundary;
- exclusive-lock, retryable/terminal checkpoint orchestration contract;
- evidence-driven Plan v1.3 Web UI and stale-server detection.

The pre-commit component rehearsal over `[2020-01-01, 2020-01-08)` rebuilt T12 from T10 and
strictly passed T13/T14/T15 consumer read-back: T12 wrote 6,089 total path/index/quality/lineage
rows, T13 and T14 wrote 2,422 rows each, and T15 aggregated the current T14 matrices. It found and
fixed a receipt-count bug where JSON-only T15 evidence was initially reported as zero rows.
The T11 component rehearsal classified all 42 Primary T10 Episodes as `DECLARED_GAP` after
verifying the bound Stage 1 partition receipts and hashes, produced 168 funding-track rows and 84
independent policy-admission decisions, and did not fabricate a win/loss for censored paths.
These are pre-commit component checks, not the mandatory final-code rehearsal.

The first clean-commit final rehearsal at `0c27e41` stopped during T12 strict read-back because
the external macOS volume created `._*.parquet` AppleDouble metadata sidecars. The inventory
correctly did not accept them as Parquet, but it had not yet classified them as filesystem
metadata. The failed rehearsal root remains unpublished and non-reusable. The producer inventory
now excludes only `._` sidecars while continuing to hash and strictly read every declared data
file; a regression test covers this exact external-volume scenario. A new commit requires the
whole rehearsal to restart from T11.

No Authority, Binning Snapshot, Run ID or publication was created.

CR-2026-038 additionally authorizes a separate real seven-day funding acceptance rehearsal. It
must compare existing BTC/ETH rows to checksummed Binance official monthly archives, write an
isolated append-only Manifest/Catalog/Verify chain and pass strict consumer read-back. It does not
authorize the lifecycle Run and cannot close the whole-history funding gate by itself.

## CR-2026-038 real funding rehearsal

The final producer and consumer ran over `[2020-01-01, 2020-01-08)` using the existing local
BTCUSDT/ETHUSDT candidates and Binance official January 2020 monthly archives.

Result:

| Instrument | Local rows | Official/accepted rows | Set differences | Cause |
| --- | ---: | ---: | ---: | --- |
| BTCUSDT | 21 | 21 | 6 | three legacy timestamps rounded down by 1-2ms |
| ETHUSDT | 21 | 21 | 6 | three legacy timestamps rounded down by 1-2ms |

CR-2026-038 behaved as designed: official timestamps were written to the new accepted copy, both
legacy inputs retained their original SHA-256, and no source was overwritten. The apparent six
differences are three missing official identities plus three extra rounded legacy identities.
Funding rates themselves agree after timestamp alignment.

Append-only evidence:

- evidence ID: `s2p13-t11-funding-7d-cr-2026-038-v1`
- root:
  `/Volumes/FuckingLife/era100x_stage2/funding-evidence/s2p13-t11-funding-7d-cr-2026-038-v1`
- Manifest Hash: `6fb8e129a8fa6d21813c485a1441b3c248fc7867c7e3b2f657e307a5304ad23f`
- Catalog Hash: `5e6fa5012fe003de7124e4960962fa43c7932a7cd3c698710f8a4016bbb9f4c3`
- Verify Hash: `f9f873678a3a9ef06063ff458c6d72b306f70c1a11d53cc2e783e1105d5f2dfa`
- independent Verify: `PASS`, 42/42 rows
- lifecycle Authority/Run created: `false`
- Stage 3: `LOCKED`

Muce explicitly waived the whole-history monthly official comparison and separate preflight
binding requirement. The local BTC/ETH histories each contain 7,128 unique rows, have one
eight-hour gap value, cover 2020-01-01 through 2026-07-03 16:00 UTC and retain their recorded
SHA-256. Acceptance and independent acceptance Verify PASS with Hash
`91ebd6e5433829a0085bc2cb9871d33d383f56a9626ac7c57b79abc7e6127d89`.
`HISTORICAL_FUNDING` is now satisfied, with the explicit limitation that no claim of monthly
row-by-row official reconciliation is made.

## CR-2026-038 quality and UI

- funding + governance + progress projection directed tests: `59 passed`;
- full quality gate: Ruff format/lint PASS, strict mypy PASS over 224 source files, strict
  Traceability PASS, governance PASS and `678 passed`;
- the first post-acceptance quality-gate attempt found one stale rerunner-test expectation that
  still listed OQ-S2-010; it was corrected and the complete gate was rerun from the beginning;
- browser projection: `PASS / SEVEN_DAY_REHEARSAL / 42 rows / 12 set differences / Verify PASS /
  acceptance PASS / bound YES`;
- UI reports `input blockers NONE`; no lifecycle Run exists and `Stage 3 LOCKED`.

## Real seven-day availability rehearsal

The first final-code attempt correctly blocked because the exclusive warmup boundary classified the
last daily-offset anchor (`2020-01-01T01:00:09Z`) as unknown. The boundary was corrected to cover
the complete 61st one-minute bar and the whole seven-day audit was rerun from a fresh root.

Commands:

```text
uv run python scripts/audit_stage2_v13_availability.py run \
  --root /tmp/era-s2-v13-availability-7d-rerun.3pssKP \
  --start-date 2020-01-01 --end-date-exclusive 2020-01-08

uv run python scripts/audit_stage2_v13_availability.py verify \
  --report /tmp/era-s2-v13-availability-7d-rerun.3pssKP/\
cac3cf2c3f8aca8dd049c52925244daab88e8e1317cd1409aaa3ade2d5780b62.json
```

Result:

| Instrument | Grid | Available | Boundary warmup | Unbound | Invalid | Unknown |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| BTCUSDT | 10,080 | 10,019 | 61 | 0 | 0 | 0 |
| ETHUSDT | 10,080 | 10,019 | 61 | 0 | 0 | 0 |

Audit and Verify status are PASS. Audit Hash:
`cac3cf2c3f8aca8dd049c52925244daab88e8e1317cd1409aaa3ade2d5780b62`.

This rehearsal validates only the T15-style price/feature availability consumer. It is not the
mandatory final `T20 → T11 → T12 → T13/T14 → T15 → T16` rehearsal.

## Blocking upstream evidence

A read-only inventory of the fixed T10 snapshot
`df15b9cbb208a6f921b3a68bee24be44f77e83eb2c8ac1582ef942b108708d33`,
the Stage 1/2 external roots and repository registries found no bound historical:

- funding-rate dataset;

The fixed snapshot does contain `contract_price_1s`, `causal_price_bars`,
`trade_second_primitives`, Trades indexes and Group-1 event datasets. Those are enough for a
price-only path. CR-2026-036/ADR-S2-016 approve that path as `CONTRACT_PRICE_H3_PROXY`, without
claiming historical Mark. CR-2026-037/ADR-S2-017 define theoretical liquidation as Contract-Price
net margin depletion at -8U, so no historical bracket is required. Missing funding still prevents
the approved Primary multi-day cost evidence. Zero funding is forbidden.

CR-2026-036/ADR-S2-016 also resolve the target ambiguity: T2 20bp is auxiliary only, while
continuation closes at dynamic net ticket doubling. The zero-funding main-cost threshold is about
136bp; accumulated funding raises the exact threshold.
Primary uses signed historical funding. Stress separately transforms positive payments to 1.5x/2x
and reports a no-credit case; every Stress row still requires the historical source binding.

## Current decision

`NO-GO` for Authority, bins or formal Run. OQ-S2-009/010/011 are resolved. CR-2026-040 moves the
already-defined seven-day requirement out of the OQ list and into the independent
`FINAL_CODE_7_DAY_REHEARSAL` execution gate. That gate is authorized but not yet run. The separate
whole-history pre-audit is waived, not the formal Run's complete-range validation and fail-closed
reconciliation. S2P13-T17～T21 were not executed and Stage 3 remains locked.

The CR-2026-040 governance reclassification passed 58 directed tests and the complete quality gate
with 679 tests. The UI must show open questions `NONE` and
`FINAL_CODE_7_DAY_REHEARSAL=PENDING`; this is not a lifecycle PASS.
