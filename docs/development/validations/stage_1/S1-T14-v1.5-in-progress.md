# S1-T14 v1.5 Validation — IN PROGRESS

- Conclusion: IN_PROGRESS (not PASS)
- Prior run: `stage1-v1.0-20260713T144630Z-c25c2a4b9b94-d44c1368`, 134/162, INVALIDATED, unpublished.
- Builder: `stage1-trades-v2`, canonical identity, conflict retention, official monthly/daily set verification, v2 manifest/catalog/hash.
- Reuse: checksum-valid raw only; prior staging forbidden.
- 2026-07-14 pipeline update: raw acquisition/checksum validation now uses six bounded
  download threads while two symbol-isolated build processes consume ready archives.
  BTC and ETH download order is interleaved but each symbol's build chronology remains
  fixed. Truncated HTTP responses preserve `.part` and resume by Range instead of
  deleting progress.
- Checkpoint additions: `prefetched_archives` and `download_workers`; download/build
  failures are persisted with archive key and phase before propagation. These fields
  are execution evidence only and do not change `stage1-trades-v2` semantics.
- Targeted validation: `tests/data/full_build/test_full_build.py` — 14 passed. Includes
  download/build overlap, symbol round-robin, truncated-response resume, Trade Identity
  v2, deterministic ordering, conflict validation and atomic checkpoint regression.
- Full locked-environment validation:
  - `PATH=.venv/bin:$PATH python3.12 scripts/run_quality_gate.py` — PASS; Ruff format,
    Ruff lint, mypy strict and 113 pytest tests passed.
  - `PATH=.venv/bin:$PATH python3.12 scripts/check_traceability.py --strict` — PASS;
    32 rules, 41 INV, 18 contracts, 52 reasons and 10 gates.
  - `PATH=.venv/bin:$PATH PYTHONPATH=src python3.12 scripts/check_contract_coverage.py`
    — PASS; Appendix C-E 15/15.
  - `git diff --check` — PASS.
- Failed environment attempt retained: invoking system `python3.12` without the project
  `.venv` failed mypy/import checks because locked Polars/PyYAML typing and the installed
  `src` package were unavailable. The same commands passed in the approved project
  environment above; the failed attempt is not represented as a test pass.
- Full-data execution was not restarted by this change. Pipeline performance and all
  162-archive output gates remain unverified on a real run; conclusion stays IN_PROGRESS.
- Remaining gate: merge/push main, launch new main-commit run, complete 162 archives, publish BTC/ETH, deterministic rebuild and full-data quality evidence.
