# S0-T07 Validation

Acceptance conclusion: PASS

- Rules: `PNL-NO-DOUBLE-SLIPPAGE`; `RESEARCH-H3-CONDITIONAL-ROUND-PROB` is represented only by formula field boundaries and was not enabled or optimized.
- Implementation: Decimal-only proxy and realized long PnL, ticket-equity guards, target exit price, and `required_target_bps` pure functions.
- Failure paths: float/non-finite/negative inputs, zero notional, invalid fee denominator, external cash flow, and pre-flat/pre-cleanup final equity are rejected.
- Tests: fixed Appendix F calculations, UT-PNL-015 no-double-slippage API, proxy/realized separation, quantity-target monotonicity property, and final-equity guards.
- Commands: `python3.12 -m pytest tests/foundation/accounting -q` passed (5); `python3.12 scripts/run_quality_gate.py` passed (45 tests, Ruff and mypy clean); `ruff format --check .` and `git diff --check` exited 0.
- Deferred scope: replay, Round state, cost selection, optimization, and trading behavior.
- Open questions: none blocking this Task.
