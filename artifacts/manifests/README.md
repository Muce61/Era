# Manifests

Future approved Tasks write immutable experiment/data manifests here. S0-T06 defines only the
contract and append-only behavior; no research or market-data manifest is generated in Stage 0.
